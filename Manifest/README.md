# Manifest

`PROMPT_SEQUENCE.csv` — человекочитаемый порядок запуска.

`PROMPT_SET_MANIFEST.json` — версия набора, текущее состояние, размеры и SHA-256
файлов на момент сборки. Сам manifest не включает собственный SHA-256, чтобы не
создавать рекурсивную хеш-змею, которая ест свой хвост.
