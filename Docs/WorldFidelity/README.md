# Historical world-fidelity evidence

This directory preserves evidence from the retired pre-v5 Milestone 06B
production-cell fidelity gate.

It is historical input, not the active world plan. The current feature-parity
workflow is:

```text
06B1 donor baseline audit and sanitation
  -> 06B2 streaming cellization and active donor profile
  -> 06B3 full-map validation and baseline freeze
```

The custom `cell_0_-3` and `cell_0_-2` production visuals remain classified as
rejected for donor fidelity and may be retained only as isolated technical
fixtures. Current runtime-world and weather go/no-go decisions must come from
`Prompts/CURRENT_STATE.md` and the 06B1–06B3 reports.
