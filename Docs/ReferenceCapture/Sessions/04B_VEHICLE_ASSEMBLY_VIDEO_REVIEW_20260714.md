# 04B — review видеозахвата rear-left drum

Дата review: 2026-07-14
Классификация: `BehavioralReference`, `DiagnosticOnly`
Валидных trials: `0`

## Evidence passport

- логический путь: `Videos/Captures/My Summer Car 2026-07-14 17-06-05.mp4`;
- фактический файл предоставлен пользователем вне Git и не копировался в проект;
- SHA-256: `adffd52e9dce4171c30b969caf1102bd7ef8af9f0686ce934f2ea0ae675689af`;
- размер: `116684715` bytes;
- длительность: `117.493 s`;
- кадр: `1920x1080`;
- заявленная media frame-rate property: примерно `33.048 fps`;
- current Steam appmanifest context на момент review: build `20171487`, manifest `7688087324231130953`;
- ограничение provenance: сам MP4 не содержит проверяемого build ID, поэтому связь с build `20171487` является контекстом текущей установки, а не встроенным доказательством файла.

Review выполнен read-only: последовательное декодирование с выборкой кадров. Первые random-seek кадры были отброшены как ненадёжные из-за повторения stale frame; выводы ниже основаны только на последовательном проходе.

## Наблюдаемая последовательность

- `00:05–00:22`: loose rear-left trailing arm перемещается к колёсной арке, но успешная установка не наблюдается.
- около `00:24`: UI идентифицирует loose part как `БАРАБАННЫЙ ТОРМОЗ`.
- около `00:30`: UI идентифицирует loose part как `ЗАДНИЙ ЛЕВЫЙ РЫЧАГ`.
- `00:36–00:40`: HUD показывает выбранный ключ `11`; при наведении на соседние инструменты видны подписи ключей `9` и `10`.
- `00:40–01:35`: выполняются повторные попытки позиционировать loose arm/drum, но контролируемый installed baseline не достигается.
- `01:45–01:54`: при воздействии выбранным ключом `11` на центральный drum/bolt target появляется запрещающий символ; видимого изменения fastener stage нет.

## Вывод

Видео подтверждает только диагностический отрицательный результат: ключ `11` отклоняется целевым fastener check. Это согласуется с независимым serialized-data trace, где target `BoltPM` имеет `localScale.x=1.4`, player `Check tool` сравнивает этот scale с `ToolWrenchSize`, а child mesh ключа `14` имеет `localScale.x=1.4`.

Запись не подтверждает:

- успешный attach или detach;
- прохождение stages `0..8`;
- направление scroll input по runtime presentation;
- blocked wheel-installed removal case;
- три чистых повторения с одинаковым исходом.

Поэтому запись имеет статус `DiagnosticOnly`, число валидных trials остаётся `0`, а требования `P0-ASSEMBLY-MOUNT-RULE` и `P0-ASSEMBLY-FASTENER-SEMANTICS` остаются `Partial`.

## Следующая безопасная capture-сессия

Нужен заранее подготовленный штатным gameplay baseline: rear-left trail arm установлен и затянут, колесо снято, drum loose. Затем записать три полных attach/detach repetitions и отдельный clean fastener trial ключом `14`: положительная прокрутка должна двигать к stage `8`, отрицательная — к stage `0`. Физический смысл одного stage как оборота/угла/torque всё ещё нельзя угадывать.
